# Development, test and notebook dependencies. Not needed to run the app or
# the pipeline -- see requirements.txt for that. Kept separate so CI can
# install it without editing the production requirements file.
-r requirements.txt
pytest>=8.0
# Only notebooks/01_kpi_analysis.ipynb imports matplotlib: the dashboard draws
# with Streamlit's own charts, so the deployed app must not carry it.
matplotlib>=3.7
